[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# runner = "probe-rs run --chip STM32F103CB --connect-under-reset"
runner = "probe-rs run --chip STM32F103CB"

[build]
target = "thumbv7m-none-eabi"

[env]
DEFMT_LOG = "debug"
